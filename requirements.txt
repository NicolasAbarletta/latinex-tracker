# Core
streamlit>=1.40.0
requests>=2.31.0
pandas>=2.1.0

# Charts
plotly>=5.20.0

# PDF parsing (watchlist financials)
pdfplumber>=0.11.0

# OCR fallback for scanned, image-only filings (TRENCO, Melo/MHCH, CMBG, CMRealty...)
# Also requires the Tesseract binary installed on the system (with 'spa' language
# data); on Windows set TESSERACT_CMD if it is not on PATH.
pymupdf>=1.24.0
pytesseract>=0.3.10
pillow>=10.0.0

# Excel export
openpyxl>=3.1.0

# Analisis con Claude
anthropic>=0.40.0
python-dotenv>=1.0.0

# Peers internacionales
yfinance>=0.2.40
